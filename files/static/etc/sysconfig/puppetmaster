# Location of the main manifest
#PUPPETMASTER_MANIFEST=/etc/puppet/manifests/site.pp

# Where to log general messages to.
# Specify syslog to send log messages to the system log.
#PUPPETMASTER_LOG=syslog

# You may specify other parameters to the puppetmaster here
#PUPPETMASTER_EXTRA_OPTS=--noca

# The number of instances of puppetmaster that should be run
# If this is more than one then mongrel and apache are required
PUPPETMASTER_INSTANCES=4

# The port on which instance one of the puppetmasters listens
# when running under mongrel. Add one for each instance
PUPPETMASTER_MASTERPORT=18140
